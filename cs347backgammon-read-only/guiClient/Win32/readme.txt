The bat file assumes Java is in the system path.

The bat file expects a server address and game name as command line arguments

Example Usage:
    Create a game
        runBGGUI.bat localhost
    Join game 0
        runBGGUI.bat localhost 0
    Join game 99
        runBGGUI.bat localhost 99
